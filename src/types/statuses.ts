export enum AccountStatus {
  PENDING = 'pending',
  IN_COMPLETE = 'inComplete',
  ACTIVE = 'active',
  DISABLE = 'disable',
}

export enum BookStatus {
  AVAILABLE = 'available',
  NOT_AVAILABLE = 'notAvailable',
  SOLDOUT = 'soldOut',
}
